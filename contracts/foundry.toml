[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.20"
optimizer = true
optimizer_runs = 200

# Crucible — Foundry project for the vulnerable target, hardened reference,
# and runtime exploit. Spike B proves the drain (100 -> 0 ETH) with a real tx hash.
